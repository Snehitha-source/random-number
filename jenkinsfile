pipeline {
    agent any 

    stages {
        stage('Checkout') {
            steps {
                sh '''
                cd $WORKSPACE
                git clone https://github.com/Snehitha-source/${clone}.git
                ls -lrt ${clone}
                cd $WORKSPACE/$clone
                echo 'Pull code from repo'
               '''
            }
        }
        stage('Build') {
            steps {
                echo 'Compile'
            }
        }
        stage('Test') {
            steps {
                echo 'Run '
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploy'
            }
        }
    }
}
